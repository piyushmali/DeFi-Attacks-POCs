{
  "name": "frontrun-poc",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy ignition/modules/deploy.js",
    "deploy:localhost": "npx hardhat ignition deploy ignition/modules/deploy.js --network localhost",
    "deploy:hardhat": "npx hardhat ignition deploy ignition/modules/deploy.js --network hardhat",
    "node": "npx hardhat node"
  },
  "keywords": ["ethereum", "defi", "frontrunning", "poc", "hardhat"],
  "author": "",
  "license": "ISC",
  "description": "Proof of Concept for Frontrunning Attack on Ethereum DEX",
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "ethers": "^6.7.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.26.0"
  }
}
